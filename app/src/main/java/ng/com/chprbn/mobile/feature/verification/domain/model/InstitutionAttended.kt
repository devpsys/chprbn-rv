package ng.com.chprbn.mobile.feature.verification.domain.model

/**
 * School / training institution from the license API (`institution_attended` object).
 */
data class InstitutionAttended(
    val name: String?
)
