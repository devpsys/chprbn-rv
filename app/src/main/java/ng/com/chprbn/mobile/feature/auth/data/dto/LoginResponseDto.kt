package ng.com.chprbn.mobile.feature.auth.data.dto

/**
 * Expected API response for login.
 *
 * Adjust fields to match the real backend contract.
 */
data class LoginResponseDto(
    val accessToken: String,
    val user: UserDto
)

